--------------------------------------------------------------------------------
-- Biscuits Config
--------------------------------------------------------------------------------

require('nvim-biscuits').setup({
  cursor_line_only = true,
  default_config = {
    min_distance = 2,
    prefix_string = "   ■ "
  },
})
